FROM caddy:2.8.4

# ------ COPY CADDY CONFIG ------
COPY caddy/domains_defaultB_strict_default/config/Caddyfile /etc/caddy/Caddyfile
COPY site-content /srv

# ------ COPY KEYS ------
RUN mkdir /keys
COPY shared/cert/keys /keys