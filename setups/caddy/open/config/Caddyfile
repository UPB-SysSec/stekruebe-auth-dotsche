{
	auto_https off
	ocsp_stapling off #we don't need to revoke our own certificates here
	log default {
		output stdout
		level DEBUG
	}
}

siteA.org {
	tls /keys/siteA.pem /keys/siteA.key {
	}

	handle {
		respond "hello from the other siteA"
	}
}

siteB.org {
	tls /keys/siteB.crt /keys/siteB.key {
	}

	handle {
		respond "hello from the other siteB"
	}
}

:80 :443 {
	# everything else
	handle {
		respond 404
	}
}
